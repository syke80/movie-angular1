describe('MovieService', () => {
    it('should pass', () => {
      expect(2).to.equal(2); // Recommended
    });
    it('should fail', () => {
      expect(2).to.equal(3); // Recommended
    });
});